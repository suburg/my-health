# Задачи: Модуль «Препараты»

**Входные данные**: Проектная документация из `/specs/005-medications/`
**Предварительные требования**: plan.md, spec.md, data-model.md, contracts/, research.md, quickstart.md

**Тесты**: Включены unit-тесты для критической бизнес-логики (фильтрация, сортировка, определение статуса «принимается сейчас», навигация) согласно конституции проекта.

**Организация**: Задачи сгруппированы по пользовательским историям, чтобы обеспечить независимую реализацию и тестирование каждой истории.

## Формат: `[ID] [P?] [Story] Описание`

- **[P]**: Можно запускать параллельно (разные файлы, нет зависимостей)
- **[Story]**: Какой пользовательской истории принадлежит задача (например, US1, US2, US3)
- Включайте точные пути к файлам в описаниях

## Соглашения о путях

Один проект (Tauri + React):
- Frontend: `src/`
- Backend (Rust/Tauri): `src-tauri/src/`
- Типы: `src/types/`
- Тесты: unit-тесты рядом с исходными файлами (`__tests__/`)

---

## Фаза 1: Настройка (общая инфраструктура)

**Цель**: Инициализация структуры проекта и типов

- [x] T001 Создать структуру директорий модуля: `src/components/medications/`, `src-tauri/src/commands/`, `src-tauri/src/storage/`, `src/contracts/`
- [x] T002 [P] Добавить типы `Medication`, `MedicationsFile`, `GetMedicationsResponse`, `AddMedicationRequest`, `UpdateMedicationRequest`, `DeleteMedicationRequest` в `src/types/index.ts`

---

## Фаза 2: Фундаментальные (блокирующие предварительные условия)

**Цель**: Базовая инфраструктура, которая ДОЛЖНА быть завершена до начала реализации ЛЮБОЙ пользовательской истории

**⚠️ КРИТИЧНО**: Ни одна пользовательская история не может начаться, пока эта фаза не завершена

- [x] T003 Реализовать Rust-хранилище `src-tauri/src/storage/medication_store.rs` — чтение/запись `medications.json` с полем `schemaVersion` (FR-009), атомарная запись (временный файл → переименование), функции `load_medications()`, `save_medications()`. Обработка ошибок (FR-011): файл отсутствует — создать новый с текущей schemaVersion; файл повреждён (невалидный JSON) — вернуть ошибку с понятным сообщением; schemaVersion несовместим — вернуть ошибку с информацией о требуемой версии
- [x] T004 [P] Реализовать Tauri-команды `src-tauri/src/commands/medications.rs` — `get_medications`, `add_medication`, `update_medication`, `delete_medication` с валидацией обязательных полей
- [x] T005 [P] Реализовать IPC-сервис `src/services/medication-service.ts` — обёртки над Tauri invoke для CRUD-операций
- [x] T006 [P] Создать zod-схему валидации для `Medication` в `src/lib/validations/medication-validation.ts` — `medicationSchema` с ограничениями длины полей (name ≤ 200, category ≤ 100, dosage ≤ 100, frequency ≤ 100, activeIngredient ≤ 200, notes ≤ 500), валидация дат ДД.ММ.ГГГГ, проверка endDate ≥ startDate
- [x] T007 Реализовать утилиты `src/lib/medication-utils.ts` — `filterByActive(medications, isActive)` для фильтра «Принимаемые сейчас», `filterByName(medications, query)`, `filterByCategory(medications, category)`, `sortByStartDateDesc(medications)`, `findPrevMedication(medications, currentId, currentCategory)`, `findNextMedication(medications, currentId, currentCategory)`, `isMedicationActive(medication)` — определение статуса активности
- [x] T008 Зарегистрировать Tauri-команды в `src-tauri/src/lib.rs` (или `main.rs`)
- [x] T008a [P] Создать/проверить конфигурационный механизм для путей к данным: если уже существует (из других модулей) — убедиться что `medications.json` использует тот же механизм; если нет — создать `src/lib/config.ts` с функциями `getConfig()`, `getDataPath()` для определения путей к JSON-файлам. Пути не захардкожены, а читаются из конфига
- [x] T009 [P] Unit-тесты `src/lib/__tests__/medication-utils.test.ts` — тесты фильтрации по активности/наименованию/категории, сортировки, навигации prev/next, определения статуса «принимается сейчас»
- [x] T009a [P] Unit-тесты `src-tauri/src/storage/__tests__/medication_store_test.rs` — тесты обработки missing файла, повреждённого JSON, несовместимой schemaVersion, атомарной записи

**Контрольная точка**: Фундамент готов — CRUD препаратов работает, данные сохраняются в JSON, утилиты протестированы

---

## Фаза 3: Пользовательская история 1 — Просмотр реестра препаратов (Приоритет: P1) 🎯 MVP

**Цель**: Пользователь открывает модуль «Препараты» и видит список всех препаратов, отсортированный по дате начала, с фильтрацией по активности, наименованию и категории.

**Независимое тестирование**: Можно полностью протестировать, добавив seed-данные в `medications.json` и проверив отображение, сортировку и фильтрацию в реестре.

### Реализация для пользовательской истории 1

- [x] T010 [P] [US1] Создать компонент `src/components/medications/MedicationTile.tsx` — плитка препарата (наименование, категория, период приёма, индикатор «принимается сейчас»)
- [x] T011 [P] [US1] Создать компонент `src/components/medications/MedicationFilters.tsx` — переключатель «Принимаемые сейчас», фильтр по категории с автодополнением, фильтр по наименованию с автодополнением
- [x] T012 [US1] Создать компонент `src/components/medications/MedicationRegistry.tsx` — реестр с загрузкой данных через `getMedications`, сортировкой, мгновенной фильтрацией (фильтры комбинируются по AND), пустым состоянием
- [x] T013 [US1] Создать компонент `src/components/medications/MedicationView.tsx` — главный экран модуля (обёртка над реестром и модалкой)
- [x] T014 [US1] Добавить роутинг/навигацию к модулю «Препараты» в главном меню приложения (`src/App.tsx`)

**Контрольная точка**: Пользовательская история 1 полностью функциональна — реестр загружается, сортирует, фильтрует, отображает пустое состояние

---

## Фаза 4: Пользовательская история 2 — Ручной ввод препарата (Приоритет: P2)

**Цель**: Пользователь создаёт новую запись препарата через модальную форму с автодополнением текстовых полей и валидацией дат.

**Независимое тестирование**: Можно полностью протестировать, создав запись препарата вручную с заполнением всех полей, проверив валидацию, сохранение и корректное отображение в реестре.

### Реализация для пользовательской истории 2

- [x] T015 [P] [US2] Создать компонент `src/components/medications/MedicationAutocomplete.tsx` — текстовое поле с выпадающим списком уникальных значений из ранее введённых записей (аналог `VisitAutocomplete`)
- [x] T016 [US2] Создать компонент `src/components/medications/MedicationModal.tsx` — модальная форма создания/редактирования (наименование, категория, действующее вещество с автодополнением, дозировка, кратность, даты начала/окончания, допинформация, валидация, сохранение)
- [ ] T017 [US2] Интегрировать `MedicationModal` с `MedicationView` — состояние открытия/закрытия, обработка сохранения через `medication-service`, обновление списка после сохранения
- [ ] T018 [US2] Реализовать автодополнение для фильтров реестра — уникальные наименования и категории из текущих записей

**Контрольная точка**: Пользовательская история 2 полностью функциональна — создание/редактирование препарата с валидацией и автодополнением

---

## Фаза 5: Пользовательская история 3 — Детальная карточка препарата (Приоритет: P3)

**Цель**: Пользователь просматривает детальную карточку препарата с индикатором статуса, навигацией prev/next, редактированием и удалением.

**Независимое тестирование**: Можно протестировать, открыв детальную карточку существующего препарата, проверив отображение всех полей, навигацию и функции редактирования/удаления.

### Реализация для пользовательской истории 3

- [ ] T019 [P] [US3] Создать компонент `src/components/medications/MedicationCard.tsx` — полная карточка препарата (блоки: основная информация, приём, дополнительная информация, индикатор статуса)
- [ ] T020 [P] [US3] Создать компонент `src/components/medications/MedicationDetailPage.tsx` — детальная страница с загрузкой препарата, навигацией prev/next, редактированием, удалением с подтверждением
- [ ] T021 [US3] Интегрировать `MedicationDetailPage` с `MedicationView` — клик по плитке в реестре открывает детальную страницу, кнопки «Предыдущий»/«Следующий» навигируют по препаратам той же категории
- [ ] T022 [US3] Реализовать модальное окно подтверждения удаления препарата в `MedicationDetailPage`

**Контрольная точка**: Пользовательская история 3 полностью функциональна — карточка, статус, навигация, редактирование, удаление

---

## Фаза 6: Полировка и сквозные задачи

**Цель**: Улучшения, затрагивающие несколько пользовательских историй

- [ ] T023 [P] Добавить unit-тесты для `medication-service.ts` в `src/services/__tests__/medication-service.test.ts` — тесты CRUD-методов, моки IPC (по аналогии с doctor-visit-service.test.ts)
- [ ] T024 Обеспечить `tabular-nums` для всех числовых/датных отображений в карточках и реестре препаратов
- [ ] T025 [P] Добавить отладочное логирование операций с данными в `medication-service.ts` согласно принципу VI конституции (без названий препаратов)
- [ ] T026 Протестировать quickstart.md — запуск, проверка структуры
- [ ] T027 Финальная проверка всех пользовательских историй по сценариям приёма из spec.md

---

## Зависимости и порядок выполнения

### Зависимости фаз

- **Настройка (Фаза 1)**: Нет зависимостей — можно начинать немедленно
- **Фундаментальные (Фаза 2)**: Зависит от завершения Настройки — **БЛОКИРУЕТ** все пользовательские истории
- **Пользовательские истории (Фаза 3+)**: Все зависят от завершения Фундаментальной фазы
  - US1 (P1) → US2 (P2) → US3 (P3) — последовательная рекомендуемая выдача
  - После Фазы 2 все US могут разрабатываться параллельно
- **Полировка (Фаза 6)**: Зависит от завершения всех пользовательских историй

### Зависимости пользовательских историй

| История | Зависит от | Может начаться после |
|---------|-----------|---------------------|
| US1 — Реестр | Фаза 2 | T003-T009 завершены |
| US2 — Ручной ввод | Фаза 2 + US1 | US1 готова (нужен реестр для отображения) |
| US3 — Детальная карточка | Фаза 2 + US1 | US1 готова (нужен реестр для клика) |

### Внутри каждой пользовательской истории

- Модели/типы до сервисов
- Сервисы до компонентов UI
- Базовые компоненты до интегрированных страниц
- История завершена до перехода к следующему приоритету

### Возможности для параллелизма

- **Фаза 1**: T001-T002 — все [P], можно параллельно
- **Фаза 2**: T003-T008 можно разбить: T003 (Rust-хранилище) параллельно с T004-T006 (Tauri-команды + сервис + валидация). T008a параллельно с остальными. T009 зависит от T007, T009a зависит от T003
- **US1**: T010 (Tile) и T011 (Filters) — [P], параллельно
- **US2**: T015 (Autocomplete) параллельно с остальными
- **US3**: T019 (Card) и T020 (DetailPage) — [P], параллельно
- **Полировка**: T023, T024, T025 — [P], параллельно

---

## Пример параллельного запуска: Пользовательская история 1

```bash
# Параллельно — разные файлы:
Задача: "Создать MedicationTile в src/components/medications/MedicationTile.tsx"
Задача: "Создать MedicationFilters в src/components/medications/MedicationFilters.tsx"

# После завершения — зависимость:
Задача: "Создать MedicationRegistry (зависит от T010, T011)"
```

---

## Стратегия реализации

### Сначала MVP (US1 — Реестр)

1. Завершить Фазу 1: Настройка
2. Завершить Фазу 2: Фундаментальные (КРИТИЧНО — блокирует все истории)
3. Завершить Фазу 3: Пользовательская история 1
4. **ОСТАНОВИТЬСЯ и ПРОВЕРИТЬ**: Реестр загружается, сортирует, фильтрует
5. Развернуть/продемонстрировать

### Инкрементальная выдача

1. Фаза 1 + Фаза 2 → CRUD и утилиты готовы
2. + US1 → Реестр работает → MVP!
3. + US2 → Ручной ввод с автодополнением → Демо
4. + US3 → Детальная карточка с навигацией → Демо
5. Фаза 6 → Полировка, тесты, проверка quickstart

### Стратегия параллельной команды

С несколькими разработчиками:

1. Команда завершает Настройку + Фундаментальные вместе
2. После завершения Фундаментальных:
   - Разработчик A: US1 (Реестр) → US3 (Карточка)
   - Разработчик B: US2 (Ввод)
3. Истории завершаются и интегрируются независимо

---

## Сводка

| Метрика | Значение |
|---------|----------|
| Всего задач | 29 |
| Фаза 1 (Настройка) | 2 |
| Фаза 2 (Фундаментальные) | 9 |
| US1 — Реестр | 5 |
| US2 — Ручной ввод | 4 |
| US3 — Детальная карточка | 4 |
| Фаза 6 (Полировка) | 5 |
| Параллельные задачи | 10 (T001-T002, T004-T006, T008a, T009-T009a, T010-T011, T015, T019-T020, T023-T025) |
| Unit-тесты | 3 набора (T009 utils, T009a store, T023 service) |
