# План реализации: [FEATURE]

**Ветка**: `[###-feature-name]` | **Дата**: [DATE] | **Спецификация**: [ссылка]
**Входные данные**: Спецификация функции из `/specs/[###-feature-name]/spec.md`

**Примечание**: Этот шаблон заполняется командой `/speckit.plan`. См. `.specify/templates/plan-template.md` для рабочего процесса выполнения.

## Сводка

[Извлечь из спецификации функции: основное требование + технический подход из исследования]

## Технический контекст

<!--
  ТРЕБУЕТСЯ ДЕЙСТВИЕ: Замените содержимое этого раздела техническими деталями
  для проекта. Структура здесь представлена в рекомендательном порядке для руководства
  процессом итерации.
-->

**Язык/Версия**: [например, Python 3.11, Swift 5.9, Rust 1.75 или ТРЕБУЕТ УТОЧНЕНИЯ]
**Основные зависимости**: [например, FastAPI, UIKit, LLVM или ТРЕБУЕТ УТОЧНЕНИЯ]
**Хранилище**: [если применимо, например, PostgreSQL, CoreData, файлы или Н/Д]
**Тестирование**: [например, pytest, XCTest, cargo test или ТРЕБУЕТ УТОЧНЕНИЯ]
**Целевая платформа**: [например, Linux-сервер, iOS 15+, WASM или ТРЕБУЕТ УТОЧНЕНИЯ]
**Тип проекта**: [например, библиотека/cli/web-сервис/мобильное-приложение/компилятор/десктоп-приложение или ТРЕБУЕТ УТОЧНЕНИЯ]
**Цели производительности**: [зависящие от домена, например, 1000 запр/с, 10k строк/с, 60 fps или ТРЕБУЕТ УТОЧНЕНИЯ]
**Ограничения**: [зависящие от домена, например, <200мс p95, <100МБ памяти, оффлайн-режим или ТРЕБУЕТ УТОЧНЕНИЯ]
**Масштаб/Объем**: [зависящий от домена, например, 10k пользователей, 1M LOC, 50 экранов или ТРЕБУЕТ УТОЧНЕНИЯ]

## Проверка конституции

*КОНТРОЛЬНАЯ ТОЧКА: Должна быть пройдена до исследования Phase 0. Повторная проверка после проектирования Phase 1.*

[Контрольные точки определяются на основе файла конституции]

## Структура проекта

### Документация (эта функция)

```text
specs/[###-feature]/
├── plan.md              # Этот файл (результат команды /speckit.plan)
├── research.md          # Результат Phase 0 (команда /speckit.plan)
├── data-model.md        # Результат Phase 1 (команда /speckit.plan)
├── quickstart.md        # Результат Phase 1 (команда /speckit.plan)
├── contracts/           # Результат Phase 1 (команда /speckit.plan)
└── tasks.md             # Результат Phase 2 (команда /speckit.tasks - НЕ создается /speckit.plan)
```

### Исходный код (корень репозитория)
<!--
  ТРЕБУЕТСЯ ДЕЙСТВИЕ: Замените приведенную ниже схему на конкретную структуру
  для этой функции. Удалите неиспользуемые варианты и расширьте выбранную структуру
  реальными путями (например, apps/admin, packages/something). Готовый план не
  должен содержать метки Вариантов.
-->

```text
# [УДАЛИТЬ ЕСЛИ НЕ ИСПОЛЬЗУЕТСЯ] Вариант 1: Один проект (ПО УМОЛЧАНИЮ)
src/
├── models/
├── services/
├── cli/
└── lib/

tests/
├── contract/
├── integration/
└── unit/

# [УДАЛИТЬ ЕСЛИ НЕ ИСПОЛЬЗУЕТСЯ] Вариант 2: Веб-приложение (когда обнаружены "frontend" + "backend")
backend/
├── src/
│   ├── models/
│   ├── services/
│   └── api/
└── tests/

frontend/
├── src/
│   ├── components/
│   ├── pages/
│   └── services/
└── tests/

# [УДАЛИТЬ ЕСЛИ НЕ ИСПОЛЬЗУЕТСЯ] Вариант 3: Мобильное приложение + API (когда обнаружены "iOS/Android")
api/
└── [как и backend выше]

ios/ или android/
└── [зависящая от платформы структура: модули функций, UI-потоки, платформенные тесты]
```

**Решение по структуре**: [Задокументируйте выбранную структуру и сошлитесь на реальные
каталоги, зафиксированные выше]

## Отслеживание сложности

> **Заполнять ТОЛЬКО если Проверка конституции имеет нарушения, которые нужно обосновать**

| Нарушение | Почему необходимо | Простая альтернатива отклонена потому что |
|-----------|------------|-------------------------------------|
| [например, 4-й проект] | [текущая потребность] | [почему 3 проектов недостаточно] |
| [например, паттерн Repository] | [конкретная проблема] | [почему прямой доступ к БД недостаточен] |
